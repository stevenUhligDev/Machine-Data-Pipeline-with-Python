[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "factory-agent"
version = "0.1.0"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
